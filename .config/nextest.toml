[profile.ci]
# Ensure slow tests terminate in CI to avoid hung workflows.
slow-timeout = { period = "60s", terminate-after = 2 }

[profile.ci.junit]
# Store CI test reports alongside other nextest artifacts.
path = "target/nextest/ci/junit.xml"
report-name = "steel-tests"
